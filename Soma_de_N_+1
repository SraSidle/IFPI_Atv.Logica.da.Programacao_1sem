 console.clear();
const prompt = require ('prompt-sync')();

let adcnum = +prompt('Adicione um número qualquer. '); 

let soma = (adcnum + 1) * (adcnum / 2); 

console.log(`"(${adcnum} + 1) * (${adcnum} / 2)" é igual a: ${soma}.`);
